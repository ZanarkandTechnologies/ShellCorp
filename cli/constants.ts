/**
 * CLI SHARED CONSTANTS
 * ====================
 * Physical floor dimensions shared across office-commands.ts and office-renderer.ts.
 *
 * MEMORY REFERENCES:
 * - MEM-0144 refactor: DRY extraction
 */

export const FLOOR_SIZE = 35;
export const HALF_FLOOR = FLOOR_SIZE / 2;
