# Skill: notion

Use Notion API through CLI wrappers or curl commands.

Example command pattern:
- `notion-cli tasks list`
- `notion-cli page get <id>`

If `notion-cli` is unavailable, use configured API env vars with curl.
