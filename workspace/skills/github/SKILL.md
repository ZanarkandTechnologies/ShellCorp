# Skill: github

Use GitHub CLI (`gh`) for repository operations.

Preferred commands:
- `gh issue list --limit 20`
- `gh pr list --limit 20`
- `gh pr view <id>`
- `gh issue view <id>`

Keep command output concise and summarize key findings.
