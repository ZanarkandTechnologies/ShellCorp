2026-03-13 Manage Agent Modal Visual QA

Expected UI Spec
- Layout map: modal header at top, six-tab strip beneath, overview body split into left portrait panel and right telemetry/config panel, footer actions pinned to bottom.
- Primary CTA: `Save Changes` bottom-right, amber-accented, visually stronger than `Close`.
- Visual hierarchy: portrait/name first, model stack second, telemetry cards third.
- Spacing rhythm: card padding stays consistent and the model inputs are not cramped against the telemetry stack.
- Typography: title + agent name high contrast, uppercase micro-labels for HUD flavor, supporting text secondary.
- Color and contrast: dark navy shell, sky/amber accents, readable white text on dark surfaces.
- Elevation: overview card and portrait frame should read as layered HUD plates.
- Alignment: left portrait card vertically aligned with the right-side panel stack.
- Responsiveness: portrait column collapses above the right panel at narrower widths without clipping inputs or footer buttons.

Observed Snapshot Report
- Blocked in this sandbox. I started the Vite app successfully at `http://127.0.0.1:4174/` and attempted to open the modal via the dev-only Zustand hook (`window.__SHELLCORP_APP_STORE`), but Playwright Chromium exited before capture with `FATAL:content/browser/sandbox_host_linux.cc:41 Check failed: . shutdown: Operation not permitted (1)`.
- No trustworthy screenshot or DOM snapshot was produced from the changed screen in this environment.

Diff Report + Verdict
- Verdict: FAIL
- Top 3 visual diffs: blocked before comparison; no observed geometry evidence.
- Top 3 behavior diffs: blocked before interaction validation; modal-open automation path exists but browser launch failed.
- Severity: major
- Fix directives: rerun visual QA in an environment that permits Chromium/headless browser launch, then verify dialog width, left/right column collapse, footer pinning, and tab readability at narrow viewport.
- Artifacts: Vite app reachable at `http://127.0.0.1:4174/`; Playwright launch failure captured in terminal output on 2026-03-13.
